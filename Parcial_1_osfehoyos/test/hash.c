#include <stdlib.h>
#include <stdio.h>

char * obtener_hash(char * nombre_archivo);

int main(int argc, char * argv[])
{
    exit(EXIT_SUCCESS);
}

char * obtener_hash(char * nombre_archivo)
{
    char * hash = NULL;
    return hash;
}