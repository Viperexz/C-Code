#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char * argv[])
{
	int finished = 0;
	char comando[BUFSIZ];
	printf("Tamano Bufsize: %i ",BUFSIZ);
	while(!finished)
	{
		printf("Ingrese el comando. (help =  ayuda)\n");
		printf(">");
		memset(comando,0,BUFSIZ);
		scanf("%s", comando);
		printf("Leido: %s\n",comando);
		
		split_list * l;
		l=split(comando,"\n\r\t");
		if(l->count == o)
		{
			continue;
		}
		if(strcmp l->parts[0],"exit"
		)
		


	exit(EXIT_SUCCESS);
}
